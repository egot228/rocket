# Crash Game Telegram Mini App

Мини-игра формата Crash с современным интерфейсом, балансами в **TON** и **Telegram Stars**, поддержкой пополнения и отдельной админ-выдачей средств.

## Возможности

- 🚀 Crash-механика: старт раунда, рост множителя, кэшаут до взрыва.
- 💎 Два баланса: TON и Stars.
- ➕ Пополнение баланса через API.
- 🛡️ Админ-панель: выдача TON/Stars по admin key.
- 🎨 Улучшенный glassmorphism UI для Telegram Mini App.

## Быстрый старт

```bash
npm install
npm start
```

Открой: `http://localhost:3000`

Если `3000` уже занят, приложение автоматически попробует `3001`, `3002` и т.д. (до 10 попыток).

Чтобы запустить на фиксированном порту:

```bash
PORT=4000 npm start
```

Если указанный через `PORT` порт занят, приложение завершится с понятной ошибкой.

> Для production обязательно задайте `ADMIN_KEY` через переменные окружения.

## API

- `GET /api/users/:id` — получить баланс пользователя.
- `POST /api/topup` — пополнение `{ userId, currency: "ton"|"stars", amount }`.
- `POST /api/admin/grant` — админ-выдача `{ adminKey, userId, ton, stars }`.
- `POST /api/game/start` — старт раунда `{ userId, stake, currency }`.
- `POST /api/game/cashout` — кэшаут `{ userId, currency, stake, multiplier, crashPoint }`.

## Тесты

```bash
npm test
```
